services:
  dynamodb-local:
    image: amazon/dynamodb-local:2.5.4
    container_name: handycall-dynamodb-local
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath /home/dynamodblocal/data"
    ports:
      - "8000:8000"
    volumes:
      - ./.local/dynamodb:/home/dynamodblocal/data
