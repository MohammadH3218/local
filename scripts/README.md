# Scripts

This folder contains executable utility and deployment scripts.

For written guides and troubleshooting notes, see `docs/` (especially:
`docs/azure/`, `docs/integrations/`, and `docs/aws/`).
